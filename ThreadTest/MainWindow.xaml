<Window x:Class="ThreadTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ThreadTest"
        mc:Ignorable="d" Title="MainWindow" Width="400" Height="100">
    <StackPanel Background="{Binding MyBackground}">
        <TextBlock Text="{Binding MyProp}"></TextBlock>
        <Button Command="{Binding Submit}">Do</Button>
    </StackPanel>
</Window>
